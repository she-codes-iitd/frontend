import React from "react";
import Alert from "react-bootstrap/Alert";

const Verify = () => {
	return(
		<Alert variant = "success" className="text-center">A verification email has been sent to your email id. Click on the link in email to verify...</Alert>
	)
}

export default Verify;